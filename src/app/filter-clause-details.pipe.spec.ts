import { FilterClauseDetailsPipe } from './filter-clause-details.pipe';

describe('FilterClauseDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClauseDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
